// Object

const person = {
    name: 'Ismar',
    age: 30,
    weight: 88.6,
    isAdmin: true
};
//Para mostrar um objeto basta colocar o nome dele 
console.log(person);

//Para mostrar um atributo desse objeto, precisa-se colocar o nome do objeto e o atributo, Ex: person.name;
console.log(person.name);

console.log(`${person.name} tem ${person.age} anos`);